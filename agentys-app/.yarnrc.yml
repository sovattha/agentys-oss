# Yarn 4 (berry) config — keep node_modules linker for Playwright/Vite compat.
# PnP would be smaller but breaks tools that scan node_modules directly.
nodeLinker: node-modules
yarnPath: .yarn/releases/yarn-4.14.1.cjs
