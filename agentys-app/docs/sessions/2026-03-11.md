# Session du 2026-03-11

## Branche : `main`

### Changements

**Fix TTL cache dossiers non-inbox (`routes.py`)**
- `_EMAIL_CACHE_TTL_BY_FOLDER` sent/archived/spam/trash : 1800s → 120s
- `get_sent_emails(max_age_seconds=...)` : 1800s → 120s (threshold SQLite "fresh")
- `get_by_folder(max_age_seconds=...)` : 1800s → 120s (trash/archived/spam/draft)
- Effet : email archivé ou envoyé visible dans le bon dossier sous ~5–10s au lieu de 30min

**Quality gate**
- TS6133 : `hiddenNoiseCount` destructuré mais non utilisé dans `EmailList.tsx` → supprimé
- Ruff E741 : variable ambiguë `l` → `log` dans `agent_marketplace.py` et `faq_log_repository.py`
- Ruff F401/F541 : 10 imports inutilisés + f-strings sans placeholder auto-corrigés

### Découvertes
- L'infra stale-while-revalidate était déjà correcte (background IMAP refresh + frontend revalidation au `source === "cache"`). Le seul bug était le seuil 30min qui rendait les données SQLite "toujours fraîches", bloquant le refresh.
- Ruff E741 n'est pas auto-fixable (renommage sémantique) — doit être corrigé manuellement.

### Décisions
- Seuil 2min (120s) : cohérent avec le TTL in-memory réduit de la phase 13 (60s inbox). Assez court pour qu'un envoi soit visible rapidement, assez long pour éviter des requêtes IMAP inutiles.

---

### Changements — FAQ daemon : archive SQLite + corps vide Gmail

**Fix `FAQ_AUTO_SENT` daemon (`daemon.py`)**
- Après `provider.archive_email()` : ajout de `update_folder_by_email_id(email.id, "archived")` + `emit_email_archived()` — le path REST (`routes.py`) avait déjà ces appels, pas le path daemon.
- Avant `return True` (inconditionnellement) : `delete_by_is_sent(account_id)` pour invalider le cache sent.

**Fix corps vide lors du routing (`daemon.py`)**
- `get_message_headers()` utilise `format="metadata"` → `body = ""` par design (optimisation listing).
- Le daemon appelait `router.classify_and_draft(body=email.body or "")` → FAQ score = 0 pour ces emails.
- Fix : si `email.body` est vide et que le provider supporte `get_message_by_id`, fetcher le corps complet avant le routing.

### Découvertes
- Gmail API retourne un snippet vide pour certains emails (`19cde68b92a72aad` : sujet garbage `pppp...`, corps présent dans le detail view mais absent du listing SQLite).
- Aucune entrée `faq_agent_logs` pour cet email — la classification "Noise" venait du smart routing avec corps vide, pas du FAQ agent.
- Le path daemon `FAQ_AUTO_SENT` était divergent du path REST : les 3 opérations post-envoi (SQLite folder, WebSocket, cache sent) n'existaient que dans `routes.py`.

### Décisions
- Fetch du corps complet en lazy uniquement si `email.body` est vide — pas de fetch systématique qui alourdirait tous les cycles du daemon.
