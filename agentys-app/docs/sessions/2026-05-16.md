
---

## Branche : `fix/audit-2026-05-11-deep` (TanStack migration phases 0–2)

### Changements
- Issue nathan/agentys#690 — phases 0 à 2 du refactor "unifier état client en cache TanStack Query".
- **Phase 0 (foundation, pure addition)** : install `@tanstack/react-query`. Nouveaux modules `src/lib/canonicalAccountId.ts` (normalisateur total int/hex/anon), `src/lib/queryKeys.ts` (factories `qk.settings|labels|labelCounts`, accountId toujours en position 1), `src/lib/queryClient.ts` (singleton, 30s staleTime / 30min gcTime / pas de refetch focus), `src/lib/webSocketInvalidator.ts` (scaffold vide pour Phase 5). `<QueryClientProvider>` câblé dans `main.tsx`, invalidator attaché, teardown HMR.
- **Phase 1 (settings)** : nouveau `src/api/settings.ts` (HTTP brut, auth headers conservés). `createSettingHook` réécrit autour de `useQuery` + `useMutation` (optimistic via `onMutate.setQueryData`, rollback exact via snapshot dans `onError`). Signature publique `(accountId?) => { value, toggle }` inchangée — 0 diff aux ~25 callers. `useSettingsCache.ts` devient un shim qui délègue à queryClient (préserve les 9 callers directs hors hooks : App.tsx, CalendarView, useAutoReplySetting, useBookingUrlSetting, etc.).
- **Phase 2 (labels)** : nouveau `src/hooks/useActiveAccountId.ts` (lecture réactive via `useSyncExternalStore` sur l'event existant `agentys:account-changed`). `useLabels` réécrit en `useQuery` × 2 (labels + counts) + `useMutation` (toggleFavorite). Suppression du debounce manuel + `countsFetchingRef` (lignes 42-77 de l'ancienne impl) — dédup in-flight + staleTime de queryClient couvrent la même surface. `UseLabelReturn` préservée → `LabelsProvider`, `useSharedLabels*` intacts.
- 2 tests existants patchés pour wrapper en `QueryClientProvider` (rendaient `<App />` / `<EmailContentReader />` directement, bypassant `main.tsx`).
- **Volontairement hors scope** : `broadcastQueryClient` (cross-tab pas câblé pré-migration → ajout serait un changement de comportement, pas une préservation), `persistQueryClient` (Phase 3), migration des 9 callers du shim `useSettingsCache` (préservation de la signature les laisse pour des PR ultérieurs à petit blast radius).

### Verifications
- `npx eslint` sur les fichiers touchés → 0 erreur.
- `npx tsc -b --noEmit` → 0 erreur.
- Tests dédiés : `canonicalAccountId.test.ts` (18 cas — anon bucket, int form, hex form, fallback, totality), `queryFoundation.test.tsx` (4 cas — singleton, factories, lifecycle invalidator, mount + useQuery), `createSettingHook.test.tsx` (4 cas — boot dedup ×3 hooks → 1 fetch, isolation par account, optimistic + rollback), `useLabels.test.tsx` (4 cas — boot dedup, optimistic favorite, no-fire-pré-account, account switch refetch).
- **Régression sweep ×3** comme demandé : 101 test files, 1416 passed, 326 skipped, 0 failed sur les 3 runs consécutifs. Un flake (optimistic-toggle dans createSettingHook.test.tsx) attrapé au 2/3 — rewrite vers `waitFor` au lieu de `Promise.resolve` microtask → stable sur les runs suivants.

---

## Branche : `main` (i18n lazy-split — issue nathan/agentys#687)

### Changements
- Issue #687 — split des locales par langue via Vite dynamic imports (custom `BackendModule` i18next), au lieu de la proposition `i18next-http-backend` (qui aurait cassé HMR + introduit du fetch HTTP inutile sur l'app Tauri).
- `src/i18n/index.ts` réécrit : `import.meta.glob('./locales/*/*.json')` → 1 chunk par langue, active language preloadée synchronement avant `createRoot()` (zéro flash de keys). Exports publics : `lazyBackend`, `NAMESPACES`, `SUPPORTED_LANGS`, `i18nReady`.
- `vite.config.ts` : règle `manualChunks` qui coalesce les namespaces d'une langue en `locale-<lng>.js` (sinon 52 micro-chunks).
- `src/__tests__/setup.ts` : ancien comportement (le module `./i18n` chargeait EN/DE/ES via le `else` HMR-branch en re-import) déménagé explicitement ici en eager `import.meta.glob({ eager: true })`. Sinon les ~25 tests qui font `i18n.changeLanguage('en')` rendraient en FR (régression silencieuse).
- `src/__tests__/i18n.lazy.test.ts` (nouveau) : 5 régressions — preload active lang, changeLanguage trigger backend, fallback FR sur clé manquante, glob coverage (lang × ns), langue inconnue ne hang pas.
- `package.json` + `yarn.lock` : retrait de `i18next-browser-languagedetector` (jamais importé nulle part dans `src/`).

### Verifications
- Build : main chunk 1247 KB → 550 KB raw ; 4 locale chunks séparés (87-93 KB gzip chacun). Boot d'un user FR : -386 KB raw vs avant (3 chunks langues non chargés).
- Live (Vite preview + Playwright) : seul `locale-fr-*.js` chargé au boot, titre rendu en FR, zéro EN/DE/ES dans `performance.getEntriesByType('resource')`.
- `i18n.lazy.test.ts` : 5/5 passed.
- Tests régressions (i18n.lazy + SwipeableEmailItem + notifications + EmailContentReader) : 79/79 passed.
- Lint sur les 4 fichiers TS touchés : 0 erreur.
- **Non-régression hors scope** : la suite complète remonte 3 fails dans `Wizard.test.tsx` (`api.getEnvConfig is not a function`) — pré-existant dans le merge `1d7a1dd3`, sans rapport avec ce changement. Idem typecheck (erreurs dans `App.tsx` ↔ `deep-focus.ts` WIP non-tracké).
