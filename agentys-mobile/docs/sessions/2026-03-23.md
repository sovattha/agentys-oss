# Session du 2026-03-23

## Branche : `main`

### Changements
- **Logo mobile** : remplacement du `logo.png` (triangle plein cyan + cercle glow) par `AgentysLogo.tsx` — composant SVG inline via WebView, 3 triangles imbriqués en contour teal (`#3DBEAC`), aucune dépendance ajoutée
- **preview-all-screens.html** : mis à jour pour refléter le nouveau logo (SVG inline au lieu du CSS border-trick + divs glow)
- **Inbox mobile** : refonte majeure (`app/(tabs)/inbox.tsx`, ~430 lignes supprimées) — simplification de la structure
- **agentys-app** : correctifs audit phase 2 — i18n étendue (common.json +79 clés FR/EN), dateFormat refactorisé, Settings.css ajouté, EmailList amélioré
- **Backend** : routes_emails +325 lignes (endpoints nettoyage emails), routes_pending +27, labels +49

### Décisions
- `AgentysLogo` utilise WebView + SVG inline plutôt que `react-native-svg` (non installé) — cohérent avec l'approche existante de `PyramidAnimation`
- La couleur `#3DBEAC` (teal) diverge volontairement du `primary` cyan `#22d3ee` pour correspondre à la charte logo brand

### Problèmes rencontrés
- Après la modification de `login.tsx`, le logo affiché restait l'ancien : l'utilisateur visualisait `preview-all-screens.html` (fichier HTML statique), pas l'app Expo. Correction : mise à jour du fichier HTML en plus du code React Native.
