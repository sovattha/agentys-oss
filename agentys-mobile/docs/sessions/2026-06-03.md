# Session du 2026-06-03

## Branche : `fix/e2e-audit-2026-06-02`

### Changements
Refonte du turn-taking vocal du mode conduite (agentys-mobile) — corrige les
collisions « l'AI parle / l'utilisateur parle ». L'AI ne parle plus que du
contenu ; le protocole passe en signaux non-verbaux. Plan complet + checklist
Step 0 : `docs/voice-first-redesign.md`.

- **Step 1 — earcons (ON).** Nouveau `src/hooks/useEarcons.ts` + 4 assets WAV
  (`assets/earcons/`, générés par `scripts/gen-earcons.py`). Vocabulaire
  turn/tick/done/alert remplace les ~8 prompts parlés (`tts.shouldSend`,
  `tts.askDraft`) ; tick sur `onVoiceStart` (feedback <100ms pendant le trou
  STT batch) ; earcons sur archive/delete/send. Haptiques gardés en doublon ;
  `StatusPill` reste le backstop visuel. Coût TTS RÉDUIT (earcons = $0).
- **Step 2 — full-duplex (OFF, flag).** `FULL_DUPLEX_READS` dans
  `useDriveMode.ts` : `configureForVoiceChat()` une fois par session +
  lectures de brouillon via `speakAndListen`. Par défaut OFF = comportement
  identique à aujourd'hui. À NE PAS activer avant la vérif device (Step 0).
- **Step 3 — envoi en un souffle (ON).** Nouvel état `undo_window`.
  `extractSendDisposition` (strict, suffixe uniquement) détecte « …et envoie »
  / « …and send it », retire le verbe du corps, et après lecture du brouillon
  ouvre une fenêtre d'annulation de 4s (tick + chip « annuler » + micro pour
  « annule ») au lieu de demander « J'envoie ? ». `asking_send`/`asking_preview`
  intacts. Câblage `DrivePlayer` (couleur/chip/emoji) + locales en/fr.

Step 4 (streaming Deepgram WS + tap natif AVAudioEngine + suppression de
`detectTailCommand`) : DIFFÉRÉ (multi-jours, iOS + backend, non buildable
depuis Windows). Spec dans le doc.

### Verifications
- `tsc --noEmit` : clean.
- `jest` : 13/14 suites vertes — dont les nouveaux tests `useEarcons` et
  `extractSendDisposition`, les tests `useDriveMode` existants (2 assertions
  périmées `getEmailSpeakable` corrigées), et `DrivePlayer.a11y`.
- Seule suite rouge : `DrivePlayer.test.tsx` — 7 snapshots PRÉ-EXISTANTS
  périmés (états anciens, diff structurel de ~654 lignes lié au refactor
  panResponder/visualizer antérieur ; mon diff DrivePlayer = +10/−1, juste
  `undo_window`). Laissés tels quels (hors scope).
- Note harnais jest : tout mock `Audio.Sound` doit exposer `replayAsync`
  (les earcons l'appellent) — ajouté à `jest.setup.js`.
